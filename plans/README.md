# 当前状态与待办

核对日期：2026-09-05。功能总览见 [README](../README.md)，长期维护规则见 [业务规则](../docs/业务规则.md)，构建与测试见 [运行命令](../docs/运行命令.md)。

## 当前交付

课表、主动休息、超长自由专注时长修正、目标奖励回路、任务排序、导出与恢复保护、QML 静态及运行时门禁均已完成。最新功能提交为 `ad83353`，数据库 schema v13；该版本离屏验证 70/70 通过并已部署。

目前没有已批准、等待执行的编号计划。已完成计划正文与历史设计稿已清理，执行过程从 Git 历史查询。

## 待评估事项

以下事项未排期，不是执行授权。开工前重新核对当前实现、复现证据与范围。

| 事项 | 已知状态与下一步 |
| --- | --- |
| 备份 Schema 与数据域全量校验 | 已有版本感知的必要结构、课表约束及主键校验；全量数据域校验仍未完成。需要兼容旧备份，不能重复实现已有检查或无条件要求后续新增表。 |
| 备份资源预算 | 文件大小、行数、字段长度与处理耗时上限尚未形成完整方案，需先定义边界并复现。 |
| 倒计时 model reset | 历史审计接受该问题，但尚未独立稳定复现；先补复现和边界测试，再决定是否改动。 |
| 目标维度导出 | 尚未实现，是旧方案中的扩展设想；是否纳入产品仍需另行决定。 |

## 保持的决策

| 决策 | 原因与边界 |
| --- | --- |
| 统计页等级与已达成列表保持撤回 | 产品选择控制统计页信息密度，不能当作缺陷回归补回；撤回记录为 `3887154`。目标页与全局奖励继续保留。 |
| 待办课表与今日专注暂不增加切页快捷键 | 2026-09-03 明确决定；注册表没有这两个动作，用户只能修改现有动作的键位。 |
| 课表与本周计划并存 | 一个按星期和时段循环，一个锚定具体日期；课表不生成任务，不计统计。 |
| 025/027/028 的性能改造不执行 | 原有工装、列表虚拟化和页面按需加载方案缺少足够收益依据。出现新性能问题后重新测量，不沿用旧性能数字作为当前证据。 |
| 026 阴影优化需重新设计 | 原方案忽略 `blurMax` 对阴影外观的影响，不能直接执行。 |
| 030 Canvas 改 Shape 不作为性能修复 | 原方案已降为可维护性选项，缺少性能收益证据。 |

## 文档维护

- 本索引只保留当前交付、待评估事项和仍有效的决策，不累积审计流水账、旧行号和历史测试耗时。
- 新计划仅在任务明确且未完成时保留；完成后把长期规则并入业务规则或运行命令，删除执行步骤与一次性验收截图。
- 既往实施过程可用 `git log -- plans docs` 查询；本次清理前的完整资料保存在 `ad83353`，例如 `git show ad83353:plans/043-schedule-timetable.md`。历史文字不是当前执行指令。
