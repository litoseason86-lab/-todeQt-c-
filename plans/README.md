# 当前状态与待办

核对日期：2026-09-14。功能总览见 [README](../README.md)，长期维护规则见 [业务规则](../docs/业务规则.md)，构建与测试见 [运行命令](../docs/运行命令.md)。

## 进行中的计划

暂无。

## 当前交付

**045：高频动线与信息设计修复**（2026-09-13 需求挖掘，2026-09-14 **功能实现与离屏统一验收完成**，
七个阶段与统一验收补出的两处均经用户复核；全量离屏回归 85/85；分支 `work/20260913-需求挖掘`，已提交并部署到 `/Applications/番茄Todo.app`）。
计划全文（证据、被推翻的论证、对比稿实测表、各阶段验收与复核记录）随 `188580a` 入库留档后按约定删除，
查阅用 `git show 188580a:plans/045-高频动线与信息设计修复.md`：

- 专注页任务标题即选择器；⌘↩ 与全局热键在空闲时按同一口径直接开始，取不到任务才展开选择器。
  新建任务走 `TaskManager::createTask` 返回编号，不按标题反查。
- 知识缺口捕获快捷键 ⌘⇧K 与出厂留空的全局热键；窗口未激活时挂起请求（3 秒过期、可取消、执行前重判弹窗 / 录制 / 恢复阻断），
  主动休息时走窗口级无来源捕获。
- 三个弹窗的科目下拉内联新建，恢复点按科目编号记。
- 本周计划每日与每周「已排」合计、已完成沉底、列表键盘导航（游标按任务编号记，删除落点五级链，
  侧栏点击与点任务行后交接焦点，一天超高时按任务行补日内滚动偏移）。
- 专注历史月历格底 8px 投入条与目标详情热力图改为同一套四档分钟阈值（`HeatmapBands.js`），
  零值 / 今日 / 未来逐格按逻辑今日判；目标详情播报单位从「番茄」更正为分钟。

实施后审查确认的六个问题均已修复并经复核；统一验收逐条对照各阶段验收项补测，
补出并修掉一个缺陷：本周计划一天任务比视口高时，游标移到当天靠后的任务不会滚进视口
（只按天定位，缺日内偏移），这处修复与补测已经用户复核通过。
复核另指出阶段三「其它已打开弹窗同步刷新科目」对新建、编辑任务弹窗未满足（「没找到触发路径」不能替代验收），
已补齐：四个带科目下拉的弹窗都订阅 `categoriesChanged` 并按科目编号保住选中，已经用户复核通过。
长期规则已并入[业务规则](../docs/业务规则.md)的对应小节。

**未覆盖（继续保留，不写成已验证）**：没有启动应用窗口（自动流程禁止拉起 GUI），因此系统级窗口激活
（全局热键从后台真正前置窗口）、带壁纸的真实玻璃面板底、悬停态与真实数据量级下的性能都没有实机证据。
另有一条未复现、未修复的潜在风险记在「待评估事项」：其它三条延后执行的快捷键路径未重判恢复阻断。

休息时间轴、任务与时间轴维护流程、控件高度刻度统一等前几轮改动均已提交（`d463c4b`、`ea83fd0`、`7280c43`）。

知识缺口（原 044 计划）已于 2026-09-11 在分支 `work/20260911-knowledge-gaps` 上完成全部五个阶段：schema v14 与版本化备份契约、`KnowledgeGapService`、专注页与今日任务页的非模态捕获、清单页与侧栏入口、今日任务页提醒条与转任务桥接。四个决策按方案建议取定——侧栏独立页、不区分条目类型、提醒做到今日任务页提示条、删除用二次确认。长期规则已并入 [业务规则](../docs/业务规则.md) 的「知识缺口」小节，计划文件按约定删除。

同一分支上随后又修掉了知识缺口页的布局问题与说教式文案、专注页捕获钮与沉浸钮的重叠（补了几何断言并做过变异验证），并新增「设置 → 外观 → 侧栏顺序」（规则见业务规则「侧栏顺序」小节）。

合并前的代码审查确认了 9 个本分支引入的问题，已全部修复；每项先补回归用例、在旧实现上确认失败再改。修复内容：外键写成 `ON DELETE CASCADE` 的知识缺口表不再被启动与备份放行（两处共用外键契约）；「今天做」与「全部加到今天」不再重复建任务，已有没做完关联任务的条目被拒绝，提示条也不再计入；转任务不再覆盖逾期到期日；编辑弹窗不再把低优先级回填成中、不再横向越界，并跟随科目增删；清单页的 0 点日界不再被当成缺值；恢复备份后侧栏顺序立即刷新；批量转任务的失败条目单独提示数量与原因。审查暴露的测试盲区是合法零值、重复操作、恢复通知和外部数据变化——只测初始状态与首次成功调用抓不到这些。

2026-09-12 又做了一轮界面修整：知识缺口的「计划处理日期」换成日期胶囊（`DatePill`：点开是常用日期加月历，留空仍是常态，清除做在胶囊上），设置页的侧栏顺序改成拖动排序并去掉 ↑ ↓ 按钮；同时修掉两个 bug——设置页右侧的滚动条压住最右一列控件，以及侧栏在窗口重新激活后出现两个框（焦点环原来只看 `activeFocus`，现按 `visualFocus` 判定）。两项界面选择都先出了离屏对比稿再定，结果记在下面的「保持的决策」里。

全量离屏回归 79/79 通过（`~/pt-audit`），已执行 `deploy-local-app`，`/Applications/番茄Todo.app` 与构建包主二进制一致。改动已提交在分支 `work/20260911-knowledge-gaps`（不写具体哈希：提交说明里引用自己的哈希永远对不上），尚未合入 `main`。部署时有旧进程在运行且未被终止，需自行退出并重新打开才能加载新版。

实施过程中测试抓出并修掉的三个真问题，记在这里以免重犯：空 `QString` 绑定写成 SQL NULL 撞上 `NOT NULL`；`ORDER BY status ASC` 把未排期条目顶到已排期前面；事务内失败语句未 `finish()` 就 `rollback()` 会留下表锁。

### 未做的部分

- 知识缺口的 CSV 导出。`exportAll` 的多文件同事务快照要改，与本需求的闭环无关，未纳入本期。
- 系统通知级别的提醒（方案里的 L3）。先只做到今日任务页提示条，避免一上来就打扰；用一段时间后再定。
- 知识缺口页没有切页快捷键，沿用 2026-09-03 关于切页键只覆盖 8 页的决策。

### 升级注意

schema 升到 v14 后不可回退：`createTables()` 会在任何 CREATE/ALTER 之前拒绝版本更高的库，旧版本 `.app` 打开新库会直接失败。

## 待评估事项

以下事项未排期，不是执行授权。开工前重新核对当前实现、复现证据与范围。

| 事项 | 已知状态与下一步 |
| --- | --- |
| 备份 Schema 与数据域全量校验 | 已有版本感知的必要结构、课表约束及主键校验；全量数据域校验仍未完成。需要兼容旧备份，不能重复实现已有检查或无条件要求后续新增表。 |
| 备份资源预算 | 文件大小、行数、字段长度与处理耗时上限尚未形成完整方案，需先定义边界并复现。 |
| 倒计时 model reset | 历史审计接受该问题，但尚未独立稳定复现；先补复现和边界测试，再决定是否改动。 |
| 目标维度导出 | 尚未实现，是旧方案中的扩展设想；是否纳入产品仍需另行决定。 |
| 快捷键延后执行路径未重判恢复阻断 | 新建任务、开始/暂停、结束专注三条快捷键经 `Qt.callLater` 延后执行，延后那步没有再判 `backupOperationBlocksInput`。**未复现、未修复的潜在风险**，不是已证明不可达：时间窗口短不等于事件顺序有保证。 |
| 课表窄块保住周次范围 | 七列下 `narrow` 把「时间 + 周次范围」整行砍掉；节次模式下纵轴不是时间，周次范围也不是时间。建议至少给周次范围补角标。 |
| 目标详情页版式 | 内容列居中两侧大片留空；总进度条与 100 格是同一个数画两遍；一个目标看不完要滚动。外观大改，先出对比稿。 |
| 目标详情热力图翻月 | `detailYear/detailMonth` 只从逻辑今日赋值，无翻月；6 月开始的目标到 9 月只能看 9 月。 |
| 预测与截止日放到一起 | 「照此速度还需 N 天」与「截止日期」分在两处，用户要自己做减法。 |
| 100 格里程碑描边 | 只在未达成时可见，达成后被填充盖掉，且无图例。 |
| 科目管理入口移出设置 | 入口挪到侧栏还是今日页页头需单独决定。 |
| 翻页期控件统一 | 课表与统计用紧凑箭头，专注历史与本周计划用三个文字钮。外观改动，先出对比稿。 |
| 专注历史「清理无效记录」的位置 | 维护动作常驻页头，与翻月按钮平级。 |
| 本周计划给过去的日子加任务 | `canAddTaskForIndex = !isPastIndex`，补记要绕到今日页新建再改期。需先定产品边界。 |
| 中断番茄的出口 | `pomodoro_completed` 已落库但无出口，可在统计页加「完成 N / 开始 M」。低于 3 分钟的会话整段丢弃，不能写成全口径「中断率」。 |
| 统计页「全程」区间 | 统计只有今日 / 本周 / 本月，`getCategoryStats(start, end)` 本就收任意区间，只差界面开口。 |
| 知识缺口复习调度 | 现为单向流程；最小实现是 `resolveGap` 时可选「N 天后再看」，沿用 `due_date` + `reopenGap`。需先定产品边界。 |
| 考试日反推 | 倒计时与科目、长期目标不通；可算「距考试 N 天 × 日均可用分钟」对比各科缺口。 |
| 任务优先级 | `tasks` 无优先级，`knowledge_gaps` 有三档。要么补，要么写明「任务的轻重只用顺序表达」。 |
| 预设科目改为可删除 | 数据策略变更，须另立计划与迁移方案：删除 SQL、`canDeleteCategory()`、界面门禁，以及补种逻辑的墓碑标记（否则删掉会复活）。 |

## 保持的决策

| 决策 | 原因与边界 |
| --- | --- |
| 统计页等级与已达成列表保持撤回 | 产品选择控制统计页信息密度，不能当作缺陷回归补回；撤回记录为 `3887154`。目标页与全局奖励继续保留。 |
| 知识缺口行内四个操作按钮保持同分量常驻 | 2026-09-11 看过四种放法的离屏对比稿后选定；落选的是「主操作强调填充 + 次操作描边」「悬停才出现」「主操作 + ⋯ 菜单」。不要把「按钮没有主次」当缺陷重新提出；要改先出对比稿再定。 |
| 知识缺口图标用便签笔；专注页右上角两颗钮常态透明、悬停才出圆形玻璃底 | 2026-09-11 看过离屏对比稿后选定。图标落选的是灯泡、书签加号、问号气泡（原先的圆圈问号被嫌丑）；外观落选的是常驻玻璃圆钮、两钮合成玻璃胶囊。沉浸钮的「⛶」字符同时换成同一族的线性图标。要改先出对比稿再定。 |
| 计划处理日期用日期胶囊；侧栏顺序只留拖动 | 2026-09-12 看过离屏对比稿后选定。日期落选的是「输入框 + 日历按钮」；侧栏顺序去掉 ↑ ↓ 是用户明确选择——行更干净，代价是键盘调不了顺序，不要当无障碍缺陷回归补回。必填、需要手输或逐日调整的日期仍用 `DateInput`。要改先出对比稿再定。 |
| 热力图四档绝对分钟阈值；两页共用映射不共用输入 | 2026-09-14 看过 1:1 离屏稿后定稿。4 档而非 5 档是版面分组判断（5 档同样满足对比度与色差，但三档会挤在同一侧），不是度量结论。按占当月最大值分档已否决：整月学得少也会染出最深一天。目标详情与专注历史同一天落不同档是正确结果（一个统计该目标、一个统计全部）。 |
| 目标详情热力：未来格实描边、今日 accent 2px、零值保留填充；日期不降色 | 2026-09-14 定稿。未来格落选的是细描边（夜间糊掉）与不画格子（网格塌掉）。零值偏弱是定案，认不出时加专用 `heatmapZeroBackground`，不改全局 `surfaceSunken`。零值与未来日期保留 `ink`，稿里的 `inkMuted` 不到 4.5:1。 |
| 专注历史月历用格底投入条，不整格铺色 | 2026-09-14 定稿（B 方案）。整格铺色会被选中底色整个盖掉，色块压过整页；未来日期保留 `ink`，降色后夜间像不可点击。 |
| 不记题量 / 正确率 | 全应用只有「分钟」一把尺子，引入第二把会破坏口径唯一；收益不确定，破坏性明确。 |
| 本周计划不改七列横排 | 改动大，且与拖动改期冲突；已补每日合计。 |
| 课表周末列底色不调 | 深色主题下周末列实测是**更暗**约 2%，按设计刻意压暗，不是亮柱。 |
| 待办课表与今日专注暂不增加切页快捷键 | 2026-09-03 明确决定；注册表没有这两个动作，用户只能修改现有动作的键位。 |
| 课表与本周计划并存 | 一个按星期和时段循环，一个锚定具体日期；课表不生成任务，不计统计。 |
| 025/027/028 的性能改造不执行 | 原有工装、列表虚拟化和页面按需加载方案缺少足够收益依据。出现新性能问题后重新测量，不沿用旧性能数字作为当前证据。 |
| 026 阴影优化需重新设计 | 原方案忽略 `blurMax` 对阴影外观的影响，不能直接执行。 |
| 030 Canvas 改 Shape 不作为性能修复 | 原方案已降为可维护性选项，缺少性能收益证据。 |

## 文档维护

- 本索引只保留当前交付、待评估事项和仍有效的决策，不累积审计流水账、旧行号和历史测试耗时。
- 新计划仅在任务明确且未完成时保留；完成后把长期规则并入业务规则或运行命令，删除执行步骤与一次性验收截图。
- 既往实施过程可用 `git log -- plans docs` 查询；本次清理前的完整资料保存在 `ad83353`，例如 `git show ad83353:plans/043-schedule-timetable.md`。历史文字不是当前执行指令。
